GOSPEL_PAIRING_SECRET=
# Leave unset for phone tunnels so RP ID follows the Host header.
# Set to localhost only for local http://localhost testing.
# GOSPEL_RP_ID=localhost
PORT=8788
# Optional: public phone URL advertised by GET /api/bridge
# GOSPEL_TUNNEL_URL=https://your-tunnel.example
